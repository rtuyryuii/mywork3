name: Run Native Edge Automation

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-automation:
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Execute Main Orchestrator Script
        shell: powershell
        run: |
          .\test_edge_run.ps1
